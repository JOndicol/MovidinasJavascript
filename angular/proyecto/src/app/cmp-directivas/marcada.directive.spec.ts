import { MarcadaDirective } from './marcada.directive';

describe('MarcadaDirective', () => {
  it('should create an instance', () => {
    const directive = new MarcadaDirective();
    expect(directive).toBeTruthy();
  });
});
